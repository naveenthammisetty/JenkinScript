apiVersion: v1
kind: Service
metadata:
  name: jenkinsscript-service
spec:
  type: NodePort
  selector:
    app: jenkinsscript-app
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
    nodePort: 30080